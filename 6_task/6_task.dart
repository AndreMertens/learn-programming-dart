// Zufallszahlen

// Aufgabe:
// Schreibe eine Funktion, die eine int-Zufallszahl zwischen min (einschließlich) und max (ausschließlich) zurück gibt.

// Hinweis:
// Verwende die Random-Klasse aus dem Dart-Paket math, um Zufallszahlen zu generieren.

// Random random = Random();
// int randomNumber = random.nextInt(n);
// nextInt(n) gibt eine Zufallszahl zwischen 0 (einschließlich) und n (ausschließlich) zurück.

int randomInt(int min, int max) {
  return 0;
}

void main() {
  print('Random number between 5 and 10: ${randomInt(5, 10)}');
}
