// Abstand zweier Punkte

// Die Funktion soll den Abstand zweier Punkte P1 = (x1, y1), P2 = (x2, y2) berechnen.

// Hinweis:
// Abstand(P1, P2) = Wurzel((x2 - x1)^2 + (y2 - y1)^2)
// Verwende die Funktion sqrt() aus dem Dart-Paket math, um die Wurzel zu berechnen.
// import 'dart:math';

// Beispiel:
// P1(5, 4), P2(9, 7) -> Abstand: 5.0

double getDistance(double x1, double y1, double x2, double y2) {
  return 0;
}

void main() {
  double x1 = 5.0;
  double y1 = 4.0;
  double x2 = 9.0;
  double y2 = 7.0;
  print('Distance: ${getDistance(x1, y1, x2, y2)}');
}
